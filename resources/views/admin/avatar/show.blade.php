@extends('admin.layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Avatar {{ $avatar->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/avatar') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/avatar/' . $avatar->id . '/edit') }}" title="Edit Avatar"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('admin/avatar' . '/' . $avatar->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Avatar" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $avatar->id }}</td>
                                    </tr>
                                    <tr><th> Hash </th><td> {{ $avatar->hash }} </td></tr><tr><th> S3 Url Abc </th><td> {{ $avatar->s3_url_abc }} </td></tr><tr><th> S3 Url Glb </th><td> {{ $avatar->s3_url_glb }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
